import { MateriasFilterPipe } from './materias-filter.pipe';

describe('MateriasFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MateriasFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
