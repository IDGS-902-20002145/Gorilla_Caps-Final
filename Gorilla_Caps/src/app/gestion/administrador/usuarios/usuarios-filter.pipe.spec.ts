import { UsuariosFilterPipe } from "./usuarios-filter.pipe";

describe('UsuariosFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UsuariosFilterPipe();
    expect(pipe).toBeTruthy();
  });
});