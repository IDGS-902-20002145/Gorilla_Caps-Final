import { FiltroMateriasPipe } from './filtro-materias.pipe';

describe('FiltroMateriasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroMateriasPipe();
    expect(pipe).toBeTruthy();
  });
});
