import { CatalogoFilterPipe } from './catalogo-filter.pipe';

describe('CatalogoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CatalogoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});